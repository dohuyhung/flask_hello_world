# You will need to install Nginx on EC2 first.
# Copy your Nginx configuration if you need to do so via a Bash script.
# cd /tmp
# cp -f /tmp/hello.conf /etc/nginx/conf.d

# restart nginx service
systemctl restart nginx